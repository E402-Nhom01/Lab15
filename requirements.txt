# CPU-only torch wheel (~200MB) instead of default CUDA build (~750MB).
# Must come before sentence-transformers so pip picks this index first.
--extra-index-url https://download.pytorch.org/whl/cpu

streamlit>=1.32.0
altair>=5.0.0
pandas>=2.1.0
numpy>=1.24.0

# RAG runtime stack (used by app.py via agent.main_agent)
pypdf>=4.0.0
openai>=1.10.0
requests>=2.31.0
rank_bm25>=0.2.2
torch>=2.0.0
sentence-transformers>=2.7.0
